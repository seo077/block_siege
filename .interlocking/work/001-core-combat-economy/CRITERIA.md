REQ-001
  Judged: A newly created match reports exactly two players; each independently totals 100 uniquely identified blocks comprising 87 reserve blocks, one 6-structure-block fortress, one loaded 3-structure-block catapult, and one loaded 2-structure-block tank, for exactly 200 unique blocks overall; rebuilding the scenario produces the same IDs and scene bindings.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-001`

REQ-002
  Judged: Creating matches with player ID lists `[7,3]`, `[30,10,20]`, and `[20,30,10]` preserves list order and gives every entry the same identifier, reserve, fortress, weapons, and per-turn-action interface; changing one entry's action state leaves every other entry unchanged. A static dependency-boundary check starts from every file under `scripts/core/**`, recursively follows every project-local preload/load/class/autoload state dependency to a fixed point, reports the complete visited-file and dependency-edge set, and fails if any file in that transitive closure references a scene node, scene/UI/input adapter, or fixed two-player pair representation; dependencies from scene/UI/input adapters inward to the core remain allowed. A fixture that moves prohibited coupling into a newly introduced indirect helper/autoload must make the check fail, proving that nominated-file inspection cannot pass it.
  Method: Run `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-002`, then run `node tests/architecture/verify_core_state_boundary.mjs --root scripts/core --transitive --negative-self-test indirect-helper-autoload` and inspect its emitted closure/edge report.

REQ-003
  Judged: For each player, one eligible loaded weapon accepts one drag and creates exactly one projectile bearing the former ammunition block ID, immediately leaves the weapon unloaded and marked fired for that turn, and rejects inactive-player, unloaded, and repeated-fire attempts without creating another projectile.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-003`

REQ-004
  Judged: Fixed-tick cases remain resolving before 0.8 s; only settle after every tracked body stays at or below 0.12 BL/s linear and 0.2 rad/s angular speed continuously for 0.6 s; any above-threshold tick resets that interval; unresolved motion reaches timeout at exactly 8.0 s with retry available and no destruction, ownership, or victory mutation.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-004`

REQ-005
  Judged: For every structure block of every object, the match captures and stores that block's own transform only after the initial physics state is stable, before any shot or fall-threshold adjudication; a probe can enumerate the stored baseline by block ID and proves that later motion and repeated adjudication do not overwrite it. Relative to those per-block stored baselines, a structure block is fallen at displacement equal to half its 0.2 BL short side (0.1 BL) or rotation equal to 30 degrees, not just below either boundary; distinct initial transforms prove that one block's baseline is never reused for another, an object is destroyed only when every structure block is fallen, and arbitrary ammunition displacement does not affect that result.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-005`

REQ-006
  Judged: Independent no-kill, single-kill, and simultaneous-multi-kill pose fixtures transfer exactly the fired block to the defender on failure, or exactly the fired block plus every structure and loaded-ammunition block of every destroyed enemy weapon to the attacker on success; unaffected weapons remain owned by the defender and applying the same resolution twice makes no second change.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-006`

REQ-007
  Judged: After initialization, firing, successful resolution, failed resolution, multi-destruction, delete-pending handling, timeout, retry, duplicate callback, and deliberately duplicated-reference rejection, an independent enumeration of reserve, field structure, loaded ammunition, and active projectile IDs contains exactly 200 unique known IDs with no missing, extra, or multiply located ID; rejected transactions leave the full snapshot unchanged.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-007`

REQ-008
  Judged: Two- and three-player fixtures begin at round 1 with the first list entry active, advance exactly once through list order, and increment only after the last entry; round 20 ends immediately after the last turn without creating round 21 and selects by total owned blocks, then fortress-owned blocks, then draw. Independently supplied poses ending all, but not merely some, enemy fortress blocks end the match immediately for the attacker without changing those poses.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-008`

REQ-009
  Judged: While resolving and while timed out, new fire, turn end, and duplicate result callbacks are rejected. At each 8.0 s timeout, independently captured projectile/target transforms, linear/angular velocities, active player, round, weapon/turn flags, all IDs, and ownership remain identical and bodies are frozen. Retry restores that same shot, creates no shot or transfer, resets only elapsed and continuous-quiet time to zero, then either times out under the same preservation rule or settles once and applies exactly one result.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-009`

REQ-010
  Judged: Two consecutive runs through initialization, guarded firing, threshold/failure/success/multi-destruction, fortress victory, ordered turns/round-20 scoring, timeout/retry, UI diagnostics, and independent 200-ID accounting all pass under the repository-local Godot 4 executable; the play scene visibly reports adjudication state and independently counted total blocks.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-010 --repeat 2`

REQ-011
  Judged: Instantiating the real play scene in initial, resolving, and timeout states yields a visible state label and total equal to a separate enumeration of scene bodies and match-state IDs; timeout visibly supplies an enabled error/Retry control, and activating it preserves the shot ID, resumes resolving, and updates the displayed state with an explicit pass/fail result.
  Method: `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-010-UI`

REQ-012
  Judged: At execution time the verifier reads the approved `.interlocking/work/001-core-combat-economy/SPEC.md`, locates REQ-012 inside the normative markers, and directly extracts its ordered Korean string/format list as the sole expected oracle; it does not accept a harness or implementation constant as authority. Source and Web export decode as UTF-8, and a real browser's HUD plus `globalThis.__BLOCK_SIEGE_TEST__` match that extracted list character-for-character, contain neither U+FFFD nor known UTF-8-to-legacy corruption, and expose every required bridge field. The run writes passing JSON and PNG evidence and exits nonzero for source, export, runtime, extraction, or missing-field mismatch; a coordinated negative case that changes both runtime strings and any harness-side replica to the same corrupt values still fails against the unchanged SPEC-derived oracle.
  Method: First run the exact standard command, with no additional flags: `node tests/web/run_browser_regression.mjs --base-url http://127.0.0.1:8060 --serve build/web --requirements REQ-012,REQ-013 --evidence build/web-evidence/local --oracle-spec .interlocking/work/001-core-combat-economy/SPEC.md`. After it succeeds, separately run `node tests/web/run_browser_regression.mjs --base-url http://127.0.0.1:8060 --serve build/web --requirements REQ-012,REQ-013 --evidence build/web-evidence/oracle-negative --oracle-spec .interlocking/work/001-core-combat-economy/SPEC.md --negative-oracle-self-test coordinated-runtime-harness-corruption`; the second invocation passes only when it observes the deliberately coordinated runtime/harness corruption being rejected with a nonzero nested verification result.

REQ-013
  Judged: Fresh-game browser cases sending real canvas press/move/release events produce no shot at 23 px and exactly one at 24 px; left/right drags change aim with matching sign, upward drag increases elevation, 40/120/240 px impulse is strictly increasing, and equivalent drags for both players point toward the opponent from their own camera. JSON evidence contains received coordinates, shot ID/count, initial velocity, impulse, normalized direction, active player, and ready-to-resolving state; any violated boundary, direction, output, or bridge case exits nonzero.
  Method: `node tests/web/run_browser_regression.mjs --base-url http://127.0.0.1:8060 --serve build/web --requirements REQ-012,REQ-013 --evidence build/web-evidence/local --oracle-spec .interlocking/work/001-core-combat-economy/SPEC.md`

REQ-014
  Judged: A network-capable verifier passes the standard deployed command only when a fresh load of `https://seo077.github.io/block_siege/` shows the canvas, exact Korean HUD, one real pointer shot and `ready→resolving`, and its JSON/PNGs record a successful Pages run, live manifest, complete live-asset response hashes, and a live manifest byte-for-byte equal to the approved `build/web/build-manifest.json` with matching assets. Only an explicit policy-level network denial permits the offline route. Before preserved evidence can count, an offline verifier must use a verifier-owned GitHub/Sigstore custom trusted root supplied through a path outside the repository (and reject a root or root hash sourced from the repository), validate the artifact-attestation bundle's certificate chain, signature, and Rekor inclusion proof, and record that root's external origin and freshly computed SHA-256. It requires exact repository `seo077/block_siege`, workflow identity `.github/workflows/pages.yml`, ref `refs/heads/master`, and a signed payload digest equal to a freshly computed digest of the complete preserved evidence payload; it proves with Git that manifest `source_commit` is an ancestor of attested workflow `head_sha` rather than requiring equality, reads `build/web/build-manifest.json` from the attested head tree, and requires those exact bytes to equal both the preserved live manifest and approved local manifest. It then verifies every preserved live asset hash and required JSON/PNG and independently reruns canvas/HUD/pointer/`ready→resolving` locally against that same export. Missing, unsigned, untrusted, expired/invalid, repository-supplied-root, root-origin/hash, identity/ref/ancestry/tree-byte/payload-digest, incomplete, tampered, or contradictory evidence fails, as does an ordinary network/HTTP error; independent negative fixtures must corrupt each binding and demonstrate rejection.
  Method: Normally run `node tests/web/run_browser_regression.mjs --base-url https://seo077.github.io/block_siege/ --requirements REQ-012,REQ-013,REQ-014 --manifest build/web/build-manifest.json --evidence build/web-evidence/deployed --oracle-spec .interlocking/work/001-core-combat-economy/SPEC.md`. Only after recording policy denial, set `IL_SIGSTORE_TRUSTED_ROOT` to the verifier-owned custom-root file's absolute path outside the repository and `IL_SIGSTORE_TRUSTED_ROOT_ORIGIN` to its independently supplied origin, then run `node tests/web/verify_preserved_deployment.mjs --requirements REQ-014 --evidence build/web-evidence/deployed --attestation build/web-evidence/deployed/attestation.sigstore.json --trusted-root "$env:IL_SIGSTORE_TRUSTED_ROOT" --trusted-root-origin "$env:IL_SIGSTORE_TRUSTED_ROOT_ORIGIN" --record-trusted-root-sha256 --repository seo077/block_siege --workflow .github/workflows/pages.yml --ref refs/heads/master --manifest build/web/build-manifest.json --require-source-ancestor-of-head --require-manifest-bytes-from-attested-head --network-diagnostic build/web-evidence/network-policy.json`, then run `node tests/web/run_browser_regression.mjs --base-url http://127.0.0.1:8060 --serve build/web --requirements REQ-012,REQ-013,REQ-014 --manifest build/web/build-manifest.json --evidence build/web-evidence/independent-local --oracle-spec .interlocking/work/001-core-combat-economy/SPEC.md`. Separately run `node tests/web/verify_preserved_deployment.mjs --requirements REQ-014 --fixtures tests/web/fixtures/attestation-negative --negative-self-test all --trusted-root "$env:IL_SIGSTORE_TRUSTED_ROOT" --trusted-root-origin "$env:IL_SIGSTORE_TRUSTED_ROOT_ORIGIN"`; every coordinated signature/external-root/root-origin/root-hash/repository/workflow/ref/ancestry/attested-tree-manifest/payload-digest and evidence/hash corruption fixture must be rejected.

REQ-015
  Judged: From identical fresh fixed scenarios for both players, a zero-lateral 240 px catapult drag launches toward the opponent and its projectile center reaches x>=20 BL for player 1 or x<=-20 BL for player 2 within 8.0 s. For each player, independently sampled opponent-direction maximum progress for 40, 120, and 240 px drags is strictly increasing, and no sample initially or subsequently progresses behind its own firing direction.
  Method: Run `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-015`, inspecting the emitted per-player time/position and maximum-progress samples.

REQ-016
  Judged: Two deterministic fresh-match fixtures are both exercised: (1) player 1's active catapult with a camera-relative zero-lateral 240 px horizontal press→move→release drag toward player 2, and (2) after legally advancing the fresh match to player 2, player 2's active catapult with the mirrored camera-relative zero-lateral 240 px horizontal drag toward player 1. Each shot avoids timeout, and crossing x=±30 BL, z=±15 BL, or y<-2 BL freezes it on that fixed tick while keeping it tracked; resolution still honors the 0.8 s minimum and continuous 0.6 s quiet interval, applies ownership/victory exactly once, and ends ready or final. Enter cannot change turns while resolving; after each ready result the HUD explicitly includes `[Enter]: 턴 종료`. Boundary inside/equal/outside cases prove the specified crossing behavior independently of the two end-to-end shots.
  Method: Run `.\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe --headless --path . --script res://tests/regression_runner.gd -- --requirement REQ-016`; require named output for both `player-1-catapult-240px-zero-lateral` and `player-2-catapult-240px-zero-lateral`, plus every x/z/y boundary, duplicate-resolution, resolving-Enter, and post-resolution-HUD subcase.

REQ-017
  Judged: Fresh local and deployed lifecycle runs of the identical approved export each show an actual player-1 canvas 240 px press/move/release shot reaching x≥20 BL within 8.0 s with timestamped positions, no timeout, `resolving→ready`, pre-resolution Enter blocked, and exactly one post-resolution Enter changing player 1 to 2 while round 1 and refreshed player-2 camera/HUD/enabled input are observed, with complete JSON and PNGs. Only explicit policy network denial permits preserved deployed lifecycle evidence, and it can count only after the REQ-014 offline verifier authenticates its artifact-attestation bundle using the verifier-owned custom trusted root supplied outside the repository, records that root's external origin and computed SHA-256, exactly binds `seo077/block_siege`, `.github/workflows/pages.yml`, and `refs/heads/master`, proves manifest `source_commit` is a Git ancestor of attested workflow `head_sha`, requires the exact `build/web/build-manifest.json` bytes in that attested head tree to match preserved-live and approved-local manifest bytes, and binds the freshly computed complete lifecycle-payload digest. The independent verifier must rerun the entire lifecycle locally against that same export and reject missing/contradictory fields or screenshots, repository-supplied-root, root-origin/hash, signature/trust/identity/ref/ancestry/tree-byte/digest/hash mismatch, or ordinary network failure; negative fixtures must demonstrate all such rejection paths.
  Method: Normally run `node tests/web/run_browser_regression.mjs --base-url http://127.0.0.1:8060 --serve build/web --requirements REQ-015,REQ-016,REQ-017 --evidence build/web-evidence/local-e2e` and `node tests/web/run_browser_regression.mjs --base-url https://seo077.github.io/block_siege/ --requirements REQ-015,REQ-016,REQ-017 --manifest build/web/build-manifest.json --evidence build/web-evidence/deployed-e2e`. Only after recorded policy denial, run the local command with evidence path `build/web-evidence/independent-local-e2e`; set `IL_SIGSTORE_TRUSTED_ROOT` to the verifier-owned custom-root file's absolute path outside the repository and `IL_SIGSTORE_TRUSTED_ROOT_ORIGIN` to its independently supplied origin; then run `node tests/web/verify_preserved_deployment.mjs --requirements REQ-014,REQ-017 --evidence build/web-evidence/deployed-e2e --attestation build/web-evidence/deployed-e2e/attestation.sigstore.json --trusted-root "$env:IL_SIGSTORE_TRUSTED_ROOT" --trusted-root-origin "$env:IL_SIGSTORE_TRUSTED_ROOT_ORIGIN" --record-trusted-root-sha256 --repository seo077/block_siege --workflow .github/workflows/pages.yml --ref refs/heads/master --manifest build/web/build-manifest.json --require-source-ancestor-of-head --require-manifest-bytes-from-attested-head --network-diagnostic build/web-evidence/network-policy.json --compare-local build/web-evidence/independent-local-e2e`. Separately run `node tests/web/verify_preserved_deployment.mjs --requirements REQ-014,REQ-017 --fixtures tests/web/fixtures/attestation-negative --negative-self-test all --trusted-root "$env:IL_SIGSTORE_TRUSTED_ROOT" --trusted-root-origin "$env:IL_SIGSTORE_TRUSTED_ROOT_ORIGIN"`; all coordinated authenticity, external-root/root-origin/root-hash, ancestry, attested-tree-manifest, payload, hash, and lifecycle-contradiction fixtures must be rejected.
