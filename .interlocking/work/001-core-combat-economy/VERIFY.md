# VERIFY — 001-core-combat-economy @ HEAD
## Independence evidence
verifier: il-verifier / subagent / fresh context, implementation conversation not read
rights: read + execute; no write except this file
input: SPEC.md + CRITERIA.md + git diff 2d6b45c..HEAD
ran: git diff 2d6b45c..HEAD + read-only source/fixture inspection + `rg` core-model absence check -> completed; no executable verification command configured

## Verdict
REQ-001 | PASS | `scripts/core/match_state.gd:9-12,37-78` deterministically creates per player 6 fortress blocks, loaded 3+1 catapult, loaded 2+1 tank, and 87 reserve blocks with monotonically unique IDs; `tests/regression_runner.gd:114-139` independently asserts 100 per player, 200 total, identical reinitialization IDs, and 200 scene bodies.
REQ-002 | PASS | `scripts/core/player_state.gd:6-21` defines one uniform player item holding id/reserve/fortress/weapons/turn actions and `scripts/core/match_state.gd:14,37-78,228-232` stores/looks up an arbitrary ordered player list; `rg` over `scripts/core` for Node/P1/P2/player_1/player_2 returned `NO_MATCHES`, while `tests/regression_runner.gd:148-168` covers reordered 2- and 3-player lists and isolated action state.
REQ-003 | PASS | `scripts/main.gd:276-295` maps mouse press/release around a drag to firing; `scripts/core/match_state.gd:169-188` rejects inactive/unloaded/repeat/locked requests and atomically reuses the ammo ID as the active projectile while disarming and marking the weapon fired; `scripts/main.gd:400-469` spawns exactly that existing body.
REQ-004 | PASS | `scripts/core/resolution_state.gd:4-8,27-48,57-71` implements the 0.8/0.6/8.0 fixed-tick gates, inclusive 0.12/0.2 thresholds, any-target reset, exact timeout error, and retry availability; `scripts/main.gd:297-346` advances only from `_physics_process`, snapshots before adjudication, freezes targets, and exposes the timeout.
REQ-005 | PASS | `scripts/core/block_record.gd:9-13,24-37` stores a baseline per block and applies the inclusive 0.1 BL/30-degree boundaries; `scripts/core/weapon_state.gd:23-32` and `scripts/core/player_state.gd:29-38` require every structure ID and never consult ammo; boundary, cross-baseline, partial/all, and ammo-exclusion assertions are at `tests/regression_runner.gd:246-282`.
REQ-006 | PASS | `scripts/core/resolution_transaction.gd:11-37` collects every fully destroyed enemy weapon's structure/ammo blocks and routes the projectile to attacker on any hit or first defender on a miss; `scripts/core/resolution_transaction.gd:46-118` validates the whole plan, applies it once, rolls back invalid ledgers, and rejects duplicate shot keys without reapplying.
REQ-007 | PASS | `scripts/core/match_state.gd:237-290` independently validates every original ID as exactly one reserve/fortress/weapon/ammo/projectile claim with no missing/new IDs; `scripts/core/resolution_transaction.gd:46-118` prevalidates, applies synchronously, validates, and fully rolls back before returning any rejected snapshot, with duplicate idempotence at lines 48 and 95.
REQ-008 | PASS | `scripts/core/match_state.gd:71-105` starts at list item 0, advances in list order, increments only after the last item, and adjudicates rather than creating round 21; `scripts/core/match_state.gd:107-167` makes fortress victory/final results immutable and applies total-owner then fortress-count scoring; `tests/regression_runner.gd:441-500` covers A/B/C ordering, partial/all fortress, round-19/20 boundaries, both tie-break levels, and draw.
REQ-009 | FAIL | Criterion: "해결 중과 타임아웃 오류 중 ... 해결 콜백을 중복 전달해도 ... 결과 적용 횟수가 변하지 않는다." Actual: `scripts/core/match_state.gd:190-197` allows `resolve_shot_once()` whenever no timeout exists, and `scripts/core/resolution_transaction.gd:15-16,46-50,95-99` treats the normal `resolving_shot == true` state as valid and applies/clears the shot; the fixture only spams callbacks after entering timeout (`tests/regression_runner.gd:405-416`), so a resolving-state callback changes ownership and the apply count contrary to the criterion.
REQ-010 | INCONCLUSIVE | The packet configured no executable command, so the mandated Godot headless matrix repeated twice and human desktop observation of initial/resolving/timeout UI could not be performed; `tests/regression_runner.gd:93-109`, `tests/fixtures/full_regression.gd:4-15`, and `scripts/main.gd:647-667` provide the entry point and UI implementation but do not settle actual execution/observation.
VERDICT: FAIL (8 pass, 1 fail, 1 inconclusive)
